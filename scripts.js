const ENTER_KEYCODE = 13;

const program = (() => {

    function init() {

    };

    return {
        init
    };
})();

document.addEventListener('DOMContentLoaded', () => {
    program.init();
});